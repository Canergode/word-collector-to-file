# Word Collector to File

This project continuously collects words from the user and appends each word to a text file. The program exits when the user enters 0.

## What I Learned
- How to use an infinite loop to collect repeated input
- How to append to a file inside a loop using append mode
- How to exit a program using exit() based on user input
- How to open and close a file inside a loop
